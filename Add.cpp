//This class is used by CPPObjects in it's main() function to add two numbers.
#include "Add.h"

int add(int x, int y) 
{
	return(x + y);
}

double divide(int x, int y)
{
	return((double)x / (double)y);
}